/** Layout and palette constants shared by the piano-roll renderers. */

/** MIDI pulses per quarter note the engine emits. */
export const PPQ = 480
/** Width of the piano-key sidebar in CSS pixels. */
export const PIANO_KEY_WIDTH = 40
/** Corner radius of a rendered note rectangle. */
export const NOTE_CORNER_RADIUS = 2
/** Extra semitones padded above and below the pitch range. */
export const SEMITONE_PADDING = 2
/** Height of the minimap strip in CSS pixels. */
export const MINIMAP_HEIGHT = 22

/** Playhead burgundy. */
export const PLAYHEAD_COLOR = '#8A2E3E'
/** Cold dark background for the idle/cleared canvas. */
export const IDLE_BG = '#07080D'
